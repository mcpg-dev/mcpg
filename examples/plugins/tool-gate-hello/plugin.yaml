schema: mcpg.dev/plugin/v1
id: dev.mcpg.example.tool-gate-hello
name: mcpg-example-tool-gate-hello
description: |
  Canonical reference plugin for the ToolGate entity kind.
  A minimal "always allow" gate that demonstrates the
  unified declare_plugin! macro authoring shape — useful both as a
  copy-paste starting point for new plugin authors and as a smoke
  target for the gateway's `mcpg dev --plugin <path>` mode.
class: tool_gate
runtime: native-cdylib-v1
protocol_version: '1.0'
required_capabilities: []
