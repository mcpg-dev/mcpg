# Auto-generated sample — rebuilt T19-01 for correct mcpg schema.
gateway:
  server:
    bind_address: "127.0.0.1:8799"
    mcp_path: "/mcp"
    health_path: "/health"
    max_request_body_mb: 4

mcp:
  capabilities:
    tools:
      - name: tfc.workspaces
        title: "List workspaces in an org"
        description: "List workspaces in an org"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          required: [organization]
          properties:
            organization: { type: string }
        backend:
          kind: http
          url: "https://app.terraform.io/api/v2/organizations/${arguments.organization}/workspaces"
          method: get
          headers:
            Authorization: "Bearer ${env.TFC_TOKEN}"
            Content-Type: "application/vnd.api+json"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288

      - name: tfc.workspace.show
        title: "Show a workspace"
        description: "Show a workspace"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          required: [organization, workspace]
          properties:
            organization: { type: string }
            workspace: { type: string }
        backend:
          kind: http
          url: "https://app.terraform.io/api/v2/organizations/${arguments.organization}/workspaces/${arguments.workspace}"
          method: get
          headers:
            Authorization: "Bearer ${env.TFC_TOKEN}"
            Content-Type: "application/vnd.api+json"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288
