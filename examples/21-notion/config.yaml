# Auto-generated sample — rebuilt T19-01 for correct mcpg schema.
gateway:
  server:
    bind_address: "127.0.0.1:8807"
    mcp_path: "/mcp"
    health_path: "/health"
    max_request_body_mb: 4

mcp:
  capabilities:
    tools:
      - name: notion.page.get
        title: "Get a page"
        description: "Get a page"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          required: [page_id]
          properties:
            page_id: { type: string }
        backend:
          kind: http
          url: "https://api.notion.com/v1/pages/${arguments.page_id}"
          method: get
          headers:
            Authorization: "Bearer ${env.NOTION_TOKEN}"
            Notion-Version: "2022-06-28"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288

      - name: notion.search
        title: "Search"
        description: "Arguments form the JSON body (query, filter...)"
        input_schema:
          type: object
          properties:
            query: { type: string, default: "" }
        backend:
          kind: http
          url: "https://api.notion.com/v1/search"
          method: post
          headers:
            Authorization: "Bearer ${env.NOTION_TOKEN}"
            Notion-Version: "2022-06-28"
            Content-Type: "application/json"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288
