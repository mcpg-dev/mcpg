# Auto-generated sample — rebuilt T19-01 for correct mcpg schema.
gateway:
  server:
    bind_address: "127.0.0.1:8789"
    mcp_path: "/mcp"
    health_path: "/health"
    max_request_body_mb: 4

mcp:
  capabilities:
    tools:
      - name: vercel.projects.list
        title: "List projects"
        description: "List projects"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          properties: {}
          additionalProperties: false
        backend:
          kind: http
          url: "https://api.vercel.com/v10/projects"
          method: get
          headers:
            Authorization: "Bearer ${env.VERCEL_TOKEN}"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288

      - name: vercel.deployments.list
        title: "List deployments"
        description: "List deployments"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          properties:
            limit: { type: integer, default: 20 }
        backend:
          kind: http
          url: "https://api.vercel.com/v6/deployments"
          method: get
          headers:
            Authorization: "Bearer ${env.VERCEL_TOKEN}"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288

      - name: vercel.deployment.status
        title: "Get deployment status"
        description: "Get deployment status"
        annotations: { read_only: true, idempotent: true }
        input_schema:
          type: object
          required: [deployment_id]
          properties:
            deployment_id: { type: string }
        backend:
          kind: http
          url: "https://api.vercel.com/v13/deployments/${arguments.deployment_id}"
          method: get
          headers:
            Authorization: "Bearer ${env.VERCEL_TOKEN}"
          timeout_ms: 10000
          expected_status_codes: [200]
          require_json_response: true
          max_response_bytes: 524288
